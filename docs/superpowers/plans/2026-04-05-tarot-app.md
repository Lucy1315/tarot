# 타로카드 웹앱 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 78장 라이더-웨이트 타로 덱, 20가지 스프레드, AI 해석 스트리밍을 지원하는 한국어 전용 일회성 타로 웹앱 구현

**Architecture:** Next.js 15 App Router 기반 2페이지 앱 (홈 + 리딩). 리딩 페이지는 setup→picking→result 3단계 상태 전환. OpenAI API는 서버 API 라우트를 통해 스트리밍 프록시.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS, OpenAI SDK

---

## 파일 구조

```
tarot/
├── app/
│   ├── layout.tsx              # 루트 레이아웃 (폰트, 메타데이터)
│   ├── page.tsx                # 홈 - 스프레드 선택 그리드
│   ├── globals.css             # Tailwind + 커스텀 애니메이션
│   ├── spread/
│   │   └── [type]/
│   │       └── page.tsx        # 리딩 페이지 (setup→picking→result)
│   └── api/
│       └── reading/
│           └── route.ts        # OpenAI 스트리밍 프록시
├── components/
│   ├── SpreadGrid.tsx          # 홈 스프레드 목록 그리드
│   ├── SetupForm.tsx           # 질문/역방향/선택방식 설정
│   ├── ManualPicker.tsx        # 78장 수동 선택 그리드
│   ├── SpreadLayout.tsx        # 스프레드 레이아웃 카드 배치
│   ├── CardFace.tsx            # 카드 앞면 (이미지 + 이름)
│   ├── CardBack.tsx            # 카드 뒷면
│   └── ReadingStream.tsx       # AI 해석 스트리밍 표시
├── data/
│   ├── cards.ts                # 78장 카드 데이터
│   └── spreads.ts              # 20개 스프레드 정의 (좌표 포함)
├── lib/
│   ├── types.ts                # Card, Spread, DrawnCard 타입
│   ├── shuffle.ts              # 셔플/뽑기 로직
│   └── prompt.ts               # OpenAI 프롬프트 빌더
├── __tests__/
│   ├── shuffle.test.ts         # 셔플 로직 테스트
│   └── prompt.test.ts          # 프롬프트 빌더 테스트
├── public/
│   └── cards/                  # 0.webp ~ 77.webp
├── .env.local
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

---

### Task 1: 프로젝트 스캐폴딩

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `app/layout.tsx`, `app/globals.css`, `.env.local`

- [ ] **Step 1: Next.js 15 프로젝트 생성**

```bash
cd /Users/lucy/Documents/tarot
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --turbopack --yes
```

- [ ] **Step 2: OpenAI SDK 설치**

```bash
npm install openai
```

- [ ] **Step 3: .env.local 생성**

`.env.local`:
```
OPENAI_API_KEY=sk-your-key-here
```

- [ ] **Step 4: globals.css에 커스텀 애니메이션 추가**

`app/globals.css` — Tailwind 기본 import 아래에 추가:
```css
@layer utilities {
  .card-flip {
    transition: transform 0.6s;
    transform-style: preserve-3d;
  }
  .card-flip.flipped {
    transform: rotateY(180deg);
  }
  .card-reversed {
    transform: rotate(180deg);
  }
}
```

- [ ] **Step 5: layout.tsx 한국어 설정**

`app/layout.tsx`:
```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "타로 리딩",
  description: "AI 타로 카드 해석",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ko">
      <body className="bg-white text-gray-900 min-h-screen antialiased">
        <main className="max-w-4xl mx-auto px-4 py-8">{children}</main>
      </body>
    </html>
  );
}
```

- [ ] **Step 6: 개발 서버 확인**

```bash
npm run dev
```
Expected: localhost:3000에서 기본 페이지 표시

- [ ] **Step 7: 커밋**

```bash
git init
echo "node_modules\n.next\n.env.local" > .gitignore
git add -A
git commit -m "chore: scaffold Next.js 15 project with Tailwind and OpenAI SDK"
```

---

### Task 2: 타입 정의 및 카드 데이터

**Files:**
- Create: `lib/types.ts`, `data/cards.ts`

- [ ] **Step 1: 타입 정의**

`lib/types.ts`:
```typescript
export interface Card {
  id: number;
  name: string;
  nameEn: string;
  arcana: "major" | "minor";
  suit?: "wands" | "cups" | "swords" | "pentacles";
  number: number;
  image: string;
}

export interface SpreadPosition {
  index: number;
  label: string;
  x: number;
  y: number;
  rotation?: number;
}

export interface Spread {
  id: string;
  name: string;
  description: string;
  cardCount: number;
  positions: SpreadPosition[];
}

export interface DrawnCard {
  card: Card;
  position: number;
  reversed: boolean;
}
```

- [ ] **Step 2: 78장 카드 데이터 작성**

`data/cards.ts`:
```typescript
import { Card } from "@/lib/types";

export const cards: Card[] = [
  // 메이저 아르카나 (0~21)
  { id: 0, name: "바보", nameEn: "The Fool", arcana: "major", number: 0, image: "/cards/0.webp" },
  { id: 1, name: "마법사", nameEn: "The Magician", arcana: "major", number: 1, image: "/cards/1.webp" },
  { id: 2, name: "여사제", nameEn: "The High Priestess", arcana: "major", number: 2, image: "/cards/2.webp" },
  { id: 3, name: "여황제", nameEn: "The Empress", arcana: "major", number: 3, image: "/cards/3.webp" },
  { id: 4, name: "황제", nameEn: "The Emperor", arcana: "major", number: 4, image: "/cards/4.webp" },
  { id: 5, name: "교황", nameEn: "The Hierophant", arcana: "major", number: 5, image: "/cards/5.webp" },
  { id: 6, name: "연인", nameEn: "The Lovers", arcana: "major", number: 6, image: "/cards/6.webp" },
  { id: 7, name: "전차", nameEn: "The Chariot", arcana: "major", number: 7, image: "/cards/7.webp" },
  { id: 8, name: "힘", nameEn: "Strength", arcana: "major", number: 8, image: "/cards/8.webp" },
  { id: 9, name: "은둔자", nameEn: "The Hermit", arcana: "major", number: 9, image: "/cards/9.webp" },
  { id: 10, name: "운명의 수레바퀴", nameEn: "Wheel of Fortune", arcana: "major", number: 10, image: "/cards/10.webp" },
  { id: 11, name: "정의", nameEn: "Justice", arcana: "major", number: 11, image: "/cards/11.webp" },
  { id: 12, name: "매달린 사람", nameEn: "The Hanged Man", arcana: "major", number: 12, image: "/cards/12.webp" },
  { id: 13, name: "죽음", nameEn: "Death", arcana: "major", number: 13, image: "/cards/13.webp" },
  { id: 14, name: "절제", nameEn: "Temperance", arcana: "major", number: 14, image: "/cards/14.webp" },
  { id: 15, name: "악마", nameEn: "The Devil", arcana: "major", number: 15, image: "/cards/15.webp" },
  { id: 16, name: "탑", nameEn: "The Tower", arcana: "major", number: 16, image: "/cards/16.webp" },
  { id: 17, name: "별", nameEn: "The Star", arcana: "major", number: 17, image: "/cards/17.webp" },
  { id: 18, name: "달", nameEn: "The Moon", arcana: "major", number: 18, image: "/cards/18.webp" },
  { id: 19, name: "태양", nameEn: "The Sun", arcana: "major", number: 19, image: "/cards/19.webp" },
  { id: 20, name: "심판", nameEn: "Judgement", arcana: "major", number: 20, image: "/cards/20.webp" },
  { id: 21, name: "세계", nameEn: "The World", arcana: "major", number: 21, image: "/cards/21.webp" },

  // 마이너 아르카나 - 완드 (22~35)
  { id: 22, name: "완드 에이스", nameEn: "Ace of Wands", arcana: "minor", suit: "wands", number: 1, image: "/cards/22.webp" },
  { id: 23, name: "완드 2", nameEn: "Two of Wands", arcana: "minor", suit: "wands", number: 2, image: "/cards/23.webp" },
  { id: 24, name: "완드 3", nameEn: "Three of Wands", arcana: "minor", suit: "wands", number: 3, image: "/cards/24.webp" },
  { id: 25, name: "완드 4", nameEn: "Four of Wands", arcana: "minor", suit: "wands", number: 4, image: "/cards/25.webp" },
  { id: 26, name: "완드 5", nameEn: "Five of Wands", arcana: "minor", suit: "wands", number: 5, image: "/cards/26.webp" },
  { id: 27, name: "완드 6", nameEn: "Six of Wands", arcana: "minor", suit: "wands", number: 6, image: "/cards/27.webp" },
  { id: 28, name: "완드 7", nameEn: "Seven of Wands", arcana: "minor", suit: "wands", number: 7, image: "/cards/28.webp" },
  { id: 29, name: "완드 8", nameEn: "Eight of Wands", arcana: "minor", suit: "wands", number: 8, image: "/cards/29.webp" },
  { id: 30, name: "완드 9", nameEn: "Nine of Wands", arcana: "minor", suit: "wands", number: 9, image: "/cards/30.webp" },
  { id: 31, name: "완드 10", nameEn: "Ten of Wands", arcana: "minor", suit: "wands", number: 10, image: "/cards/31.webp" },
  { id: 32, name: "완드 시종", nameEn: "Page of Wands", arcana: "minor", suit: "wands", number: 11, image: "/cards/32.webp" },
  { id: 33, name: "완드 기사", nameEn: "Knight of Wands", arcana: "minor", suit: "wands", number: 12, image: "/cards/33.webp" },
  { id: 34, name: "완드 여왕", nameEn: "Queen of Wands", arcana: "minor", suit: "wands", number: 13, image: "/cards/34.webp" },
  { id: 35, name: "완드 왕", nameEn: "King of Wands", arcana: "minor", suit: "wands", number: 14, image: "/cards/35.webp" },

  // 마이너 아르카나 - 컵 (36~49)
  { id: 36, name: "컵 에이스", nameEn: "Ace of Cups", arcana: "minor", suit: "cups", number: 1, image: "/cards/36.webp" },
  { id: 37, name: "컵 2", nameEn: "Two of Cups", arcana: "minor", suit: "cups", number: 2, image: "/cards/37.webp" },
  { id: 38, name: "컵 3", nameEn: "Three of Cups", arcana: "minor", suit: "cups", number: 3, image: "/cards/38.webp" },
  { id: 39, name: "컵 4", nameEn: "Four of Cups", arcana: "minor", suit: "cups", number: 4, image: "/cards/39.webp" },
  { id: 40, name: "컵 5", nameEn: "Five of Cups", arcana: "minor", suit: "cups", number: 5, image: "/cards/40.webp" },
  { id: 41, name: "컵 6", nameEn: "Six of Cups", arcana: "minor", suit: "cups", number: 6, image: "/cards/41.webp" },
  { id: 42, name: "컵 7", nameEn: "Seven of Cups", arcana: "minor", suit: "cups", number: 7, image: "/cards/42.webp" },
  { id: 43, name: "컵 8", nameEn: "Eight of Cups", arcana: "minor", suit: "cups", number: 8, image: "/cards/43.webp" },
  { id: 44, name: "컵 9", nameEn: "Nine of Cups", arcana: "minor", suit: "cups", number: 9, image: "/cards/44.webp" },
  { id: 45, name: "컵 10", nameEn: "Ten of Cups", arcana: "minor", suit: "cups", number: 10, image: "/cards/45.webp" },
  { id: 46, name: "컵 시종", nameEn: "Page of Cups", arcana: "minor", suit: "cups", number: 11, image: "/cards/46.webp" },
  { id: 47, name: "컵 기사", nameEn: "Knight of Cups", arcana: "minor", suit: "cups", number: 12, image: "/cards/47.webp" },
  { id: 48, name: "컵 여왕", nameEn: "Queen of Cups", arcana: "minor", suit: "cups", number: 13, image: "/cards/48.webp" },
  { id: 49, name: "컵 왕", nameEn: "King of Cups", arcana: "minor", suit: "cups", number: 14, image: "/cards/49.webp" },

  // 마이너 아르카나 - 소드 (50~63)
  { id: 50, name: "소드 에이스", nameEn: "Ace of Swords", arcana: "minor", suit: "swords", number: 1, image: "/cards/50.webp" },
  { id: 51, name: "소드 2", nameEn: "Two of Swords", arcana: "minor", suit: "swords", number: 2, image: "/cards/51.webp" },
  { id: 52, name: "소드 3", nameEn: "Three of Swords", arcana: "minor", suit: "swords", number: 3, image: "/cards/52.webp" },
  { id: 53, name: "소드 4", nameEn: "Four of Swords", arcana: "minor", suit: "swords", number: 4, image: "/cards/53.webp" },
  { id: 54, name: "소드 5", nameEn: "Five of Swords", arcana: "minor", suit: "swords", number: 5, image: "/cards/54.webp" },
  { id: 55, name: "소드 6", nameEn: "Six of Swords", arcana: "minor", suit: "swords", number: 6, image: "/cards/55.webp" },
  { id: 56, name: "소드 7", nameEn: "Seven of Swords", arcana: "minor", suit: "swords", number: 7, image: "/cards/56.webp" },
  { id: 57, name: "소드 8", nameEn: "Eight of Swords", arcana: "minor", suit: "swords", number: 8, image: "/cards/57.webp" },
  { id: 58, name: "소드 9", nameEn: "Nine of Swords", arcana: "minor", suit: "swords", number: 9, image: "/cards/58.webp" },
  { id: 59, name: "소드 10", nameEn: "Ten of Swords", arcana: "minor", suit: "swords", number: 10, image: "/cards/59.webp" },
  { id: 60, name: "소드 시종", nameEn: "Page of Swords", arcana: "minor", suit: "swords", number: 11, image: "/cards/60.webp" },
  { id: 61, name: "소드 기사", nameEn: "Knight of Swords", arcana: "minor", suit: "swords", number: 12, image: "/cards/61.webp" },
  { id: 62, name: "소드 여왕", nameEn: "Queen of Swords", arcana: "minor", suit: "swords", number: 13, image: "/cards/62.webp" },
  { id: 63, name: "소드 왕", nameEn: "King of Swords", arcana: "minor", suit: "swords", number: 14, image: "/cards/63.webp" },

  // 마이너 아르카나 - 펜타클 (64~77)
  { id: 64, name: "펜타클 에이스", nameEn: "Ace of Pentacles", arcana: "minor", suit: "pentacles", number: 1, image: "/cards/64.webp" },
  { id: 65, name: "펜타클 2", nameEn: "Two of Pentacles", arcana: "minor", suit: "pentacles", number: 2, image: "/cards/65.webp" },
  { id: 66, name: "펜타클 3", nameEn: "Three of Pentacles", arcana: "minor", suit: "pentacles", number: 3, image: "/cards/66.webp" },
  { id: 67, name: "펜타클 4", nameEn: "Four of Pentacles", arcana: "minor", suit: "pentacles", number: 4, image: "/cards/67.webp" },
  { id: 68, name: "펜타클 5", nameEn: "Five of Pentacles", arcana: "minor", suit: "pentacles", number: 5, image: "/cards/68.webp" },
  { id: 69, name: "펜타클 6", nameEn: "Six of Pentacles", arcana: "minor", suit: "pentacles", number: 6, image: "/cards/69.webp" },
  { id: 70, name: "펜타클 7", nameEn: "Seven of Pentacles", arcana: "minor", suit: "pentacles", number: 7, image: "/cards/70.webp" },
  { id: 71, name: "펜타클 8", nameEn: "Eight of Pentacles", arcana: "minor", suit: "pentacles", number: 8, image: "/cards/71.webp" },
  { id: 72, name: "펜타클 9", nameEn: "Nine of Pentacles", arcana: "minor", suit: "pentacles", number: 9, image: "/cards/72.webp" },
  { id: 73, name: "펜타클 10", nameEn: "Ten of Pentacles", arcana: "minor", suit: "pentacles", number: 10, image: "/cards/73.webp" },
  { id: 74, name: "펜타클 시종", nameEn: "Page of Pentacles", arcana: "minor", suit: "pentacles", number: 11, image: "/cards/74.webp" },
  { id: 75, name: "펜타클 기사", nameEn: "Knight of Pentacles", arcana: "minor", suit: "pentacles", number: 12, image: "/cards/75.webp" },
  { id: 76, name: "펜타클 여왕", nameEn: "Queen of Pentacles", arcana: "minor", suit: "pentacles", number: 13, image: "/cards/76.webp" },
  { id: 77, name: "펜타클 왕", nameEn: "King of Pentacles", arcana: "minor", suit: "pentacles", number: 14, image: "/cards/77.webp" },
];
```

- [ ] **Step 3: 커밋**

```bash
git add lib/types.ts data/cards.ts
git commit -m "feat: add card/spread types and 78-card data"
```

---

### Task 3: 스프레드 데이터 (20개, 좌표 포함)

**Files:**
- Create: `data/spreads.ts`

- [ ] **Step 1: 20개 스프레드 정의**

`data/spreads.ts`:
```typescript
import { Spread } from "@/lib/types";

export const spreads: Spread[] = [
  {
    id: "one-card",
    name: "원 카드",
    description: "하나의 카드로 핵심 메시지를 받습니다",
    cardCount: 1,
    positions: [
      { index: 0, label: "핵심 메시지", x: 50, y: 50 },
    ],
  },
  {
    id: "two-cards",
    name: "2카드",
    description: "두 장으로 상황과 조언을 봅니다",
    cardCount: 2,
    positions: [
      { index: 0, label: "현재 상황", x: 35, y: 50 },
      { index: 1, label: "조언", x: 65, y: 50 },
    ],
  },
  {
    id: "three-cards",
    name: "3카드",
    description: "과거, 현재, 미래를 봅니다",
    cardCount: 3,
    positions: [
      { index: 0, label: "과거", x: 25, y: 50 },
      { index: 1, label: "현재", x: 50, y: 50 },
      { index: 2, label: "미래", x: 75, y: 50 },
    ],
  },
  {
    id: "four-cards",
    name: "4카드",
    description: "상황, 원인, 조언, 결과를 봅니다",
    cardCount: 4,
    positions: [
      { index: 0, label: "현재 상황", x: 15, y: 50 },
      { index: 1, label: "원인", x: 38, y: 50 },
      { index: 2, label: "조언", x: 62, y: 50 },
      { index: 3, label: "결과", x: 85, y: 50 },
    ],
  },
  {
    id: "five-cards",
    name: "5카드",
    description: "다섯 장으로 상황을 입체적으로 봅니다",
    cardCount: 5,
    positions: [
      { index: 0, label: "현재", x: 10, y: 50 },
      { index: 1, label: "과거", x: 30, y: 50 },
      { index: 2, label: "미래", x: 50, y: 50 },
      { index: 3, label: "원인", x: 70, y: 50 },
      { index: 4, label: "잠재력", x: 90, y: 50 },
    ],
  },
  {
    id: "double-line-6",
    name: "더블 라인 6",
    description: "두 줄로 현재와 미래를 대비합니다",
    cardCount: 6,
    positions: [
      { index: 0, label: "현재 1", x: 25, y: 30 },
      { index: 1, label: "현재 2", x: 50, y: 30 },
      { index: 2, label: "현재 3", x: 75, y: 30 },
      { index: 3, label: "미래 1", x: 25, y: 70 },
      { index: 4, label: "미래 2", x: 50, y: 70 },
      { index: 5, label: "미래 3", x: 75, y: 70 },
    ],
  },
  {
    id: "double-line-7",
    name: "더블 라인 7",
    description: "두 줄 배치에 종합 카드를 더합니다",
    cardCount: 7,
    positions: [
      { index: 0, label: "현재 1", x: 25, y: 25 },
      { index: 1, label: "현재 2", x: 50, y: 25 },
      { index: 2, label: "현재 3", x: 75, y: 25 },
      { index: 3, label: "미래 1", x: 25, y: 55 },
      { index: 4, label: "미래 2", x: 50, y: 55 },
      { index: 5, label: "미래 3", x: 75, y: 55 },
      { index: 6, label: "종합", x: 50, y: 85 },
    ],
  },
  {
    id: "double-line-8",
    name: "더블 라인 8",
    description: "네 장씩 두 줄로 상세하게 봅니다",
    cardCount: 8,
    positions: [
      { index: 0, label: "현재 1", x: 15, y: 30 },
      { index: 1, label: "현재 2", x: 38, y: 30 },
      { index: 2, label: "현재 3", x: 62, y: 30 },
      { index: 3, label: "현재 4", x: 85, y: 30 },
      { index: 4, label: "미래 1", x: 15, y: 70 },
      { index: 5, label: "미래 2", x: 38, y: 70 },
      { index: 6, label: "미래 3", x: 62, y: 70 },
      { index: 7, label: "미래 4", x: 85, y: 70 },
    ],
  },
  {
    id: "double-line-9",
    name: "더블 라인 9",
    description: "두 줄 배치에 종합 카드를 더합니다",
    cardCount: 9,
    positions: [
      { index: 0, label: "현재 1", x: 15, y: 25 },
      { index: 1, label: "현재 2", x: 38, y: 25 },
      { index: 2, label: "현재 3", x: 62, y: 25 },
      { index: 3, label: "현재 4", x: 85, y: 25 },
      { index: 4, label: "미래 1", x: 15, y: 55 },
      { index: 5, label: "미래 2", x: 38, y: 55 },
      { index: 6, label: "미래 3", x: 62, y: 55 },
      { index: 7, label: "미래 4", x: 85, y: 55 },
      { index: 8, label: "종합", x: 50, y: 85 },
    ],
  },
  {
    id: "celtic-cross",
    name: "켈틱 크로스",
    description: "가장 전통적인 10장 스프레드입니다",
    cardCount: 10,
    positions: [
      { index: 0, label: "현재 상황", x: 30, y: 50 },
      { index: 1, label: "장애물", x: 30, y: 50, rotation: 90 },
      { index: 2, label: "기반", x: 30, y: 80 },
      { index: 3, label: "과거", x: 10, y: 50 },
      { index: 4, label: "가능성", x: 30, y: 20 },
      { index: 5, label: "가까운 미래", x: 50, y: 50 },
      { index: 6, label: "나 자신", x: 75, y: 80 },
      { index: 7, label: "주변 환경", x: 75, y: 60 },
      { index: 8, label: "희망과 두려움", x: 75, y: 40 },
      { index: 9, label: "최종 결과", x: 75, y: 20 },
    ],
  },
  {
    id: "mini-celtic",
    name: "미니 켈틱 크로스",
    description: "켈틱 크로스의 간소화 버전입니다",
    cardCount: 6,
    positions: [
      { index: 0, label: "현재 상황", x: 30, y: 50 },
      { index: 1, label: "장애물", x: 30, y: 50, rotation: 90 },
      { index: 2, label: "과거", x: 10, y: 50 },
      { index: 3, label: "미래", x: 50, y: 50 },
      { index: 4, label: "위", x: 30, y: 20 },
      { index: 5, label: "아래", x: 30, y: 80 },
    ],
  },
  {
    id: "cross",
    name: "십자",
    description: "십자 형태로 상황을 조망합니다",
    cardCount: 5,
    positions: [
      { index: 0, label: "현재", x: 50, y: 50 },
      { index: 1, label: "왼쪽(과거)", x: 20, y: 50 },
      { index: 2, label: "오른쪽(미래)", x: 80, y: 50 },
      { index: 3, label: "위(의식)", x: 50, y: 20 },
      { index: 4, label: "아래(무의식)", x: 50, y: 80 },
    ],
  },
  {
    id: "horseshoe",
    name: "말발굽",
    description: "말발굽 형태로 흐름을 봅니다",
    cardCount: 5,
    positions: [
      { index: 0, label: "과거", x: 10, y: 70 },
      { index: 1, label: "현재", x: 25, y: 35 },
      { index: 2, label: "숨겨진 영향", x: 50, y: 20 },
      { index: 3, label: "조언", x: 75, y: 35 },
      { index: 4, label: "결과", x: 90, y: 70 },
    ],
  },
  {
    id: "magic-seven",
    name: "매직 세븐",
    description: "7장으로 상황을 깊이 분석합니다",
    cardCount: 7,
    positions: [
      { index: 0, label: "과거", x: 10, y: 70 },
      { index: 1, label: "현재", x: 25, y: 45 },
      { index: 2, label: "미래", x: 40, y: 25 },
      { index: 3, label: "조언", x: 50, y: 55 },
      { index: 4, label: "주변", x: 60, y: 25 },
      { index: 5, label: "장애물", x: 75, y: 45 },
      { index: 6, label: "결과", x: 90, y: 70 },
    ],
  },
  {
    id: "pyramid",
    name: "피라미드",
    description: "피라미드 형태로 단계별 분석합니다",
    cardCount: 6,
    positions: [
      { index: 0, label: "정점", x: 50, y: 15 },
      { index: 1, label: "2층 왼쪽", x: 35, y: 45 },
      { index: 2, label: "2층 오른쪽", x: 65, y: 45 },
      { index: 3, label: "기반 왼쪽", x: 20, y: 75 },
      { index: 4, label: "기반 중앙", x: 50, y: 75 },
      { index: 5, label: "기반 오른쪽", x: 80, y: 75 },
    ],
  },
  {
    id: "alternative",
    name: "양자택일",
    description: "두 가지 선택지를 비교합니다",
    cardCount: 5,
    positions: [
      { index: 0, label: "현재 상황", x: 50, y: 50 },
      { index: 1, label: "선택A 과정", x: 20, y: 30 },
      { index: 2, label: "선택A 결과", x: 20, y: 70 },
      { index: 3, label: "선택B 과정", x: 80, y: 30 },
      { index: 4, label: "선택B 결과", x: 80, y: 70 },
    ],
  },
  {
    id: "tandem",
    name: "탄뎀",
    description: "두 사람의 관계를 나란히 봅니다",
    cardCount: 6,
    positions: [
      { index: 0, label: "나의 과거", x: 30, y: 25 },
      { index: 1, label: "나의 현재", x: 30, y: 50 },
      { index: 2, label: "나의 미래", x: 30, y: 75 },
      { index: 3, label: "상대의 과거", x: 70, y: 25 },
      { index: 4, label: "상대의 현재", x: 70, y: 50 },
      { index: 5, label: "상대의 미래", x: 70, y: 75 },
    ],
  },
  {
    id: "relationship",
    name: "릴레이션십",
    description: "관계의 다양한 측면을 분석합니다",
    cardCount: 7,
    positions: [
      { index: 0, label: "나", x: 20, y: 30 },
      { index: 1, label: "상대", x: 80, y: 30 },
      { index: 2, label: "관계", x: 50, y: 30 },
      { index: 3, label: "나의 감정", x: 20, y: 65 },
      { index: 4, label: "상대의 감정", x: 80, y: 65 },
      { index: 5, label: "장애물", x: 50, y: 55 },
      { index: 6, label: "결과", x: 50, y: 80 },
    ],
  },
  {
    id: "cup-of-relationship",
    name: "컵 오브 릴레이션십",
    description: "관계를 더 깊이 들여다봅니다",
    cardCount: 8,
    positions: [
      { index: 0, label: "나의 의식", x: 25, y: 20 },
      { index: 1, label: "나의 무의식", x: 25, y: 45 },
      { index: 2, label: "상대의 의식", x: 75, y: 20 },
      { index: 3, label: "상대의 무의식", x: 75, y: 45 },
      { index: 4, label: "관계의 현재", x: 50, y: 30 },
      { index: 5, label: "관계의 과거", x: 35, y: 70 },
      { index: 6, label: "관계의 미래", x: 65, y: 70 },
      { index: 7, label: "종합 조언", x: 50, y: 90 },
    ],
  },
  {
    id: "yin-yang",
    name: "음양",
    description: "음과 양의 균형을 봅니다",
    cardCount: 6,
    positions: [
      { index: 0, label: "양(의식)", x: 35, y: 25 },
      { index: 1, label: "양(행동)", x: 20, y: 50 },
      { index: 2, label: "양(결과)", x: 35, y: 75 },
      { index: 3, label: "음(무의식)", x: 65, y: 25 },
      { index: 4, label: "음(감정)", x: 80, y: 50 },
      { index: 5, label: "음(결과)", x: 65, y: 75 },
    ],
  },
];
```

- [ ] **Step 2: 커밋**

```bash
git add data/spreads.ts
git commit -m "feat: add 20 spread definitions with layout coordinates"
```

---

### Task 4: 셔플 로직 + 테스트

**Files:**
- Create: `lib/shuffle.ts`, `__tests__/shuffle.test.ts`

- [ ] **Step 1: 테스트 환경 설정**

```bash
npm install -D vitest @types/node
```

`vitest.config.ts` (프로젝트 루트):
```typescript
import { defineConfig } from "vitest/config";
import path from "path";

export default defineConfig({
  test: {
    globals: true,
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname),
    },
  },
});
```

`package.json`에 스크립트 추가 (scripts 섹션):
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 2: 셔플 테스트 작성**

`__tests__/shuffle.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { autoShuffle, manualPick } from "@/lib/shuffle";
import { cards } from "@/data/cards";

describe("autoShuffle", () => {
  it("returns the correct number of cards", () => {
    const result = autoShuffle(3, false);
    expect(result).toHaveLength(3);
  });

  it("returns unique cards", () => {
    const result = autoShuffle(10, false);
    const ids = result.map((d) => d.card.id);
    expect(new Set(ids).size).toBe(10);
  });

  it("assigns sequential positions starting from 0", () => {
    const result = autoShuffle(5, false);
    expect(result.map((d) => d.position)).toEqual([0, 1, 2, 3, 4]);
  });

  it("sets all reversed to false when allowReversed is false", () => {
    const result = autoShuffle(20, false);
    expect(result.every((d) => d.reversed === false)).toBe(true);
  });

  it("produces some reversed cards when allowReversed is true (statistical)", () => {
    // Run 100 draws of 20 cards; at least one run should have a reversed card
    let hasReversed = false;
    for (let i = 0; i < 100; i++) {
      const result = autoShuffle(20, true);
      if (result.some((d) => d.reversed)) {
        hasReversed = true;
        break;
      }
    }
    expect(hasReversed).toBe(true);
  });

  it("returns valid card objects from the deck", () => {
    const result = autoShuffle(5, false);
    const allIds = cards.map((c) => c.id);
    result.forEach((d) => {
      expect(allIds).toContain(d.card.id);
    });
  });
});

describe("manualPick", () => {
  it("returns cards matching the selected IDs in order", () => {
    const result = manualPick([0, 5, 21], false);
    expect(result).toHaveLength(3);
    expect(result[0].card.id).toBe(0);
    expect(result[1].card.id).toBe(5);
    expect(result[2].card.id).toBe(21);
  });

  it("assigns sequential positions", () => {
    const result = manualPick([10, 20, 30], false);
    expect(result.map((d) => d.position)).toEqual([0, 1, 2]);
  });

  it("sets all reversed to false when allowReversed is false", () => {
    const result = manualPick([0, 1, 2], false);
    expect(result.every((d) => d.reversed === false)).toBe(true);
  });
});
```

- [ ] **Step 3: 테스트 실행 — 실패 확인**

```bash
npm test
```
Expected: FAIL — `@/lib/shuffle` 모듈 없음

- [ ] **Step 4: 셔플 로직 구현**

`lib/shuffle.ts`:
```typescript
import { cards } from "@/data/cards";
import { DrawnCard } from "@/lib/types";

function cryptoRandom(): number {
  const array = new Uint32Array(1);
  crypto.getRandomValues(array);
  return array[0] / (0xffffffff + 1);
}

function fisherYatesShuffle<T>(arr: T[]): T[] {
  const shuffled = [...arr];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(cryptoRandom() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled;
}

function assignReversed(allowReversed: boolean): boolean {
  if (!allowReversed) return false;
  return cryptoRandom() < 0.5;
}

export function autoShuffle(
  cardCount: number,
  allowReversed: boolean
): DrawnCard[] {
  const shuffled = fisherYatesShuffle(cards);
  return shuffled.slice(0, cardCount).map((card, index) => ({
    card,
    position: index,
    reversed: assignReversed(allowReversed),
  }));
}

export function manualPick(
  selectedIds: number[],
  allowReversed: boolean
): DrawnCard[] {
  return selectedIds.map((id, index) => {
    const card = cards.find((c) => c.id === id);
    if (!card) throw new Error(`Card not found: ${id}`);
    return {
      card,
      position: index,
      reversed: assignReversed(allowReversed),
    };
  });
}
```

- [ ] **Step 5: 테스트 실행 — 통과 확인**

```bash
npm test
```
Expected: 모든 테스트 PASS

- [ ] **Step 6: 커밋**

```bash
git add lib/shuffle.ts __tests__/shuffle.test.ts vitest.config.ts package.json package-lock.json
git commit -m "feat: add shuffle logic with crypto random + tests"
```

---

### Task 5: 프롬프트 빌더 + 테스트

**Files:**
- Create: `lib/prompt.ts`, `__tests__/prompt.test.ts`

- [ ] **Step 1: 프롬프트 빌더 테스트 작성**

`__tests__/prompt.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { buildReadingPrompt } from "@/lib/prompt";

describe("buildReadingPrompt", () => {
  it("includes spread name and question", () => {
    const prompt = buildReadingPrompt({
      spreadName: "3카드",
      question: "이직해도 될까요?",
      cards: [
        { position: "과거", name: "The Fool", reversed: false },
        { position: "현재", name: "The Tower", reversed: true },
        { position: "미래", name: "The Sun", reversed: false },
      ],
    });
    expect(prompt).toContain("3카드");
    expect(prompt).toContain("이직해도 될까요?");
    expect(prompt).toContain("The Fool");
    expect(prompt).toContain("The Tower (역방향)");
    expect(prompt).toContain("The Sun");
  });

  it("handles empty question", () => {
    const prompt = buildReadingPrompt({
      spreadName: "원 카드",
      question: "",
      cards: [{ position: "핵심 메시지", name: "The Star", reversed: false }],
    });
    expect(prompt).toContain("원 카드");
    expect(prompt).not.toContain("질문:");
  });

  it("marks reversed cards", () => {
    const prompt = buildReadingPrompt({
      spreadName: "원 카드",
      question: "",
      cards: [{ position: "핵심 메시지", name: "Death", reversed: true }],
    });
    expect(prompt).toContain("Death (역방향)");
  });
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
npm test -- __tests__/prompt.test.ts
```
Expected: FAIL

- [ ] **Step 3: 프롬프트 빌더 구현**

`lib/prompt.ts`:
```typescript
interface ReadingCard {
  position: string;
  name: string;
  reversed: boolean;
}

interface ReadingInput {
  spreadName: string;
  question: string;
  cards: ReadingCard[];
}

export function buildReadingPrompt(input: ReadingInput): string {
  const cardList = input.cards
    .map(
      (c) =>
        `- ${c.position}: ${c.name}${c.reversed ? " (역방향)" : ""}`
    )
    .join("\n");

  const questionLine = input.question
    ? `\n질문: ${input.question}\n`
    : "";

  return `당신은 숙련된 타로 카드 리더입니다. 따뜻하고 통찰력 있는 해석을 한국어로 제공합니다.

스프레드: ${input.spreadName}${questionLine}

카드 배치:
${cardList}

위 카드 배치를 기반으로 타로 리딩을 해주세요.

규칙:
1. 각 포지션별로 카드의 의미를 해석해주세요
2. 역방향 카드는 역방향의 의미를 반영해주세요
3. 카드 간의 관계와 흐름을 설명해주세요
4. 마지막에 종합적인 메시지를 전달해주세요
5. 따뜻하고 긍정적이되, 솔직한 톤을 유지해주세요`;
}
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
npm test
```
Expected: 모든 테스트 PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/prompt.ts __tests__/prompt.test.ts
git commit -m "feat: add OpenAI prompt builder with tests"
```

---

### Task 6: API 라우트 (OpenAI 스트리밍)

**Files:**
- Create: `app/api/reading/route.ts`

- [ ] **Step 1: API 라우트 구현**

`app/api/reading/route.ts`:
```typescript
import { NextRequest } from "next/server";
import OpenAI from "openai";
import { buildReadingPrompt } from "@/lib/prompt";

export async function POST(request: NextRequest) {
  const apiKey = process.env.OPENAI_API_KEY;
  if (!apiKey) {
    return Response.json(
      { error: "API 키가 설정되지 않았습니다" },
      { status: 500 }
    );
  }

  const body = await request.json();
  const { question, spread, cards } = body as {
    question: string;
    spread: string;
    cards: { position: string; name: string; reversed: boolean }[];
  };

  const prompt = buildReadingPrompt({
    spreadName: spread,
    question: question || "",
    cards,
  });

  const openai = new OpenAI({ apiKey });

  try {
    const stream = await openai.chat.completions.create({
      model: "gpt-4o-mini",
      messages: [{ role: "user", content: prompt }],
      stream: true,
    });

    const encoder = new TextEncoder();
    const readable = new ReadableStream({
      async start(controller) {
        try {
          for await (const chunk of stream) {
            const text = chunk.choices[0]?.delta?.content || "";
            if (text) {
              controller.enqueue(encoder.encode(text));
            }
          }
          controller.close();
        } catch {
          controller.enqueue(encoder.encode("\n\n해석이 중단되었습니다."));
          controller.close();
        }
      },
    });

    return new Response(readable, {
      headers: { "Content-Type": "text/plain; charset=utf-8" },
    });
  } catch (error) {
    if (error instanceof OpenAI.APIError && error.status === 429) {
      return Response.json(
        {
          error:
            "일시적으로 해석 서비스를 이용할 수 없습니다. 잠시 후 다시 시도해주세요.",
        },
        { status: 429 }
      );
    }
    return Response.json(
      { error: "해석 중 오류가 발생했습니다" },
      { status: 500 }
    );
  }
}
```

- [ ] **Step 2: 커밋**

```bash
git add app/api/reading/route.ts
git commit -m "feat: add OpenAI streaming API route with error handling"
```

---

### Task 7: 홈 페이지 (스프레드 선택)

**Files:**
- Create: `components/SpreadGrid.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: SpreadGrid 컴포넌트**

`components/SpreadGrid.tsx`:
```tsx
import Link from "next/link";
import { spreads } from "@/data/spreads";

export default function SpreadGrid() {
  return (
    <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3">
      {spreads.map((spread) => (
        <Link
          key={spread.id}
          href={`/spread/${spread.id}`}
          className="border border-gray-200 rounded-lg p-4 hover:border-gray-400 hover:shadow-sm transition-all"
        >
          <h3 className="font-medium text-sm">{spread.name}</h3>
          <p className="text-xs text-gray-500 mt-1">{spread.cardCount}장</p>
          <p className="text-xs text-gray-400 mt-1 line-clamp-2">
            {spread.description}
          </p>
        </Link>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: 홈 페이지**

`app/page.tsx`:
```tsx
import SpreadGrid from "@/components/SpreadGrid";

export default function Home() {
  return (
    <div>
      <div className="text-center mb-8">
        <h1 className="text-2xl font-bold tracking-tight">타로 리딩</h1>
        <p className="text-gray-500 mt-2 text-sm">
          스프레드를 선택하고 AI 타로 해석을 받아보세요
        </p>
      </div>
      <SpreadGrid />
    </div>
  );
}
```

- [ ] **Step 3: 개발 서버에서 홈 페이지 확인**

```bash
npm run dev
```
Expected: localhost:3000에 20개 스프레드 그리드 표시, 클릭 시 `/spread/[type]` 이동

- [ ] **Step 4: 커밋**

```bash
git add components/SpreadGrid.tsx app/page.tsx
git commit -m "feat: add home page with spread selection grid"
```

---

### Task 8: 리딩 페이지 — SetupForm 컴포넌트

**Files:**
- Create: `components/SetupForm.tsx`

- [ ] **Step 1: SetupForm 컴포넌트**

`components/SetupForm.tsx`:
```tsx
"use client";

import { useState } from "react";

interface SetupFormProps {
  spreadName: string;
  onStart: (config: {
    question: string;
    allowReversed: boolean;
    mode: "auto" | "manual";
  }) => void;
}

export default function SetupForm({ spreadName, onStart }: SetupFormProps) {
  const [question, setQuestion] = useState("");
  const [allowReversed, setAllowReversed] = useState(false);
  const [mode, setMode] = useState<"auto" | "manual">("auto");

  return (
    <div className="space-y-6">
      <h2 className="text-xl font-bold">{spreadName}</h2>

      <div>
        <label className="block text-sm font-medium text-gray-700 mb-1">
          질문 (선택사항)
        </label>
        <input
          type="text"
          value={question}
          onChange={(e) => setQuestion(e.target.value)}
          placeholder="궁금한 것을 입력하세요"
          className="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-gray-900"
        />
      </div>

      <div className="flex items-center justify-between">
        <span className="text-sm font-medium text-gray-700">역방향 카드</span>
        <button
          type="button"
          onClick={() => setAllowReversed(!allowReversed)}
          className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${
            allowReversed ? "bg-gray-900" : "bg-gray-300"
          }`}
        >
          <span
            className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${
              allowReversed ? "translate-x-6" : "translate-x-1"
            }`}
          />
        </button>
      </div>

      <div>
        <span className="block text-sm font-medium text-gray-700 mb-2">
          선택 방식
        </span>
        <div className="flex gap-2">
          <button
            onClick={() => setMode("auto")}
            className={`flex-1 py-2 text-sm rounded-lg border transition-colors ${
              mode === "auto"
                ? "bg-gray-900 text-white border-gray-900"
                : "border-gray-300 text-gray-700 hover:border-gray-400"
            }`}
          >
            자동 셔플
          </button>
          <button
            onClick={() => setMode("manual")}
            className={`flex-1 py-2 text-sm rounded-lg border transition-colors ${
              mode === "manual"
                ? "bg-gray-900 text-white border-gray-900"
                : "border-gray-300 text-gray-700 hover:border-gray-400"
            }`}
          >
            수동 선택
          </button>
        </div>
      </div>

      <button
        onClick={() => onStart({ question, allowReversed, mode })}
        className="w-full bg-gray-900 text-white py-3 rounded-lg text-sm font-medium hover:bg-gray-800 transition-colors"
      >
        {mode === "auto" ? "셔플하기" : "카드 선택하기"}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: 커밋**

```bash
git add components/SetupForm.tsx
git commit -m "feat: add setup form with question, reverse toggle, mode selection"
```

---

### Task 9: ManualPicker 컴포넌트

**Files:**
- Create: `components/ManualPicker.tsx`, `components/CardBack.tsx`

- [ ] **Step 1: CardBack 컴포넌트**

`components/CardBack.tsx`:
```tsx
interface CardBackProps {
  selected?: number; // 선택 순서 번호 (1-based), undefined면 미선택
  onClick?: () => void;
}

export default function CardBack({ selected, onClick }: CardBackProps) {
  return (
    <button
      onClick={onClick}
      className={`relative w-16 h-28 rounded-lg border-2 transition-all ${
        selected !== undefined
          ? "border-gray-900 bg-gray-800"
          : "border-gray-300 bg-gray-700 hover:border-gray-500"
      }`}
    >
      <div className="absolute inset-1 border border-gray-500 rounded opacity-50" />
      {selected !== undefined && (
        <span className="absolute inset-0 flex items-center justify-center text-white font-bold text-lg">
          {selected}
        </span>
      )}
    </button>
  );
}
```

- [ ] **Step 2: ManualPicker 컴포넌트**

`components/ManualPicker.tsx`:
```tsx
"use client";

import { useState } from "react";
import CardBack from "./CardBack";

interface ManualPickerProps {
  cardCount: number;
  onComplete: (selectedIds: number[]) => void;
}

export default function ManualPicker({
  cardCount,
  onComplete,
}: ManualPickerProps) {
  const [selectedIds, setSelectedIds] = useState<number[]>([]);

  function toggleCard(id: number) {
    setSelectedIds((prev) => {
      if (prev.includes(id)) {
        return prev.filter((x) => x !== id);
      }
      if (prev.length >= cardCount) return prev;
      return [...prev, id];
    });
  }

  return (
    <div className="space-y-4">
      <p className="text-sm text-gray-500 text-center">
        카드를 {cardCount}장 선택하세요 ({selectedIds.length}/{cardCount})
      </p>

      <div className="overflow-x-auto pb-2">
        <div className="grid grid-cols-9 sm:grid-cols-13 gap-1.5 min-w-[600px]">
          {Array.from({ length: 78 }, (_, i) => {
            const orderIndex = selectedIds.indexOf(i);
            return (
              <CardBack
                key={i}
                selected={
                  orderIndex !== -1 ? orderIndex + 1 : undefined
                }
                onClick={() => toggleCard(i)}
              />
            );
          })}
        </div>
      </div>

      <button
        onClick={() => onComplete(selectedIds)}
        disabled={selectedIds.length !== cardCount}
        className="w-full py-3 rounded-lg text-sm font-medium transition-colors disabled:bg-gray-200 disabled:text-gray-400 bg-gray-900 text-white hover:bg-gray-800"
      >
        결과 보기
      </button>
    </div>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add components/CardBack.tsx components/ManualPicker.tsx
git commit -m "feat: add manual card picker with selection order display"
```

---

### Task 10: CardFace + SpreadLayout 컴포넌트

**Files:**
- Create: `components/CardFace.tsx`, `components/SpreadLayout.tsx`

- [ ] **Step 1: CardFace 컴포넌트**

`components/CardFace.tsx`:
```tsx
import Image from "next/image";
import { Card } from "@/lib/types";

interface CardFaceProps {
  card: Card;
  reversed: boolean;
  label: string;
}

export default function CardFace({ card, reversed, label }: CardFaceProps) {
  return (
    <div className="flex flex-col items-center gap-1">
      <div
        className={`w-20 h-36 sm:w-24 sm:h-40 relative rounded-lg overflow-hidden border border-gray-200 shadow-sm ${
          reversed ? "rotate-180" : ""
        }`}
      >
        <Image
          src={card.image}
          alt={card.name}
          fill
          className="object-cover"
          sizes="96px"
        />
      </div>
      <span className="text-xs text-gray-500">{label}</span>
      <span className="text-xs font-medium">
        {card.name}
        {reversed ? " (역)" : ""}
      </span>
    </div>
  );
}
```

- [ ] **Step 2: SpreadLayout 컴포넌트**

`components/SpreadLayout.tsx`:
```tsx
import { Spread, DrawnCard } from "@/lib/types";
import CardFace from "./CardFace";

interface SpreadLayoutProps {
  spread: Spread;
  drawnCards: DrawnCard[];
}

export default function SpreadLayout({
  spread,
  drawnCards,
}: SpreadLayoutProps) {
  return (
    <div className="relative w-full" style={{ paddingBottom: "100%" }}>
      <div className="absolute inset-0">
        {spread.positions.map((pos) => {
          const drawn = drawnCards.find((d) => d.position === pos.index);
          if (!drawn) return null;

          return (
            <div
              key={pos.index}
              className="absolute -translate-x-1/2 -translate-y-1/2"
              style={{
                left: `${pos.x}%`,
                top: `${pos.y}%`,
                transform: pos.rotation
                  ? `translate(-50%, -50%) rotate(${pos.rotation}deg)`
                  : "translate(-50%, -50%)",
              }}
            >
              <CardFace
                card={drawn.card}
                reversed={drawn.reversed}
                label={pos.label}
              />
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add components/CardFace.tsx components/SpreadLayout.tsx
git commit -m "feat: add card face and spread layout components"
```

---

### Task 11: ReadingStream 컴포넌트

**Files:**
- Create: `components/ReadingStream.tsx`

- [ ] **Step 1: ReadingStream 컴포넌트**

`components/ReadingStream.tsx`:
```tsx
"use client";

import { useEffect, useState, useRef } from "react";
import { DrawnCard, Spread } from "@/lib/types";

interface ReadingStreamProps {
  spread: Spread;
  drawnCards: DrawnCard[];
  question: string;
}

export default function ReadingStream({
  spread,
  drawnCards,
  question,
}: ReadingStreamProps) {
  const [text, setText] = useState("");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const abortRef = useRef<AbortController | null>(null);

  useEffect(() => {
    const controller = new AbortController();
    abortRef.current = controller;

    async function fetchReading() {
      try {
        const response = await fetch("/api/reading", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            question,
            spread: spread.name,
            cards: drawnCards.map((d) => ({
              position:
                spread.positions.find((p) => p.index === d.position)?.label ??
                "",
              name: d.card.nameEn,
              reversed: d.reversed,
            })),
          }),
          signal: controller.signal,
        });

        if (!response.ok) {
          const data = await response.json();
          setError(data.error || "해석 중 오류가 발생했습니다");
          setLoading(false);
          return;
        }

        const reader = response.body?.getReader();
        if (!reader) {
          setError("스트리밍을 시작할 수 없습니다");
          setLoading(false);
          return;
        }

        const decoder = new TextDecoder();
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          setText((prev) => prev + decoder.decode(value, { stream: true }));
        }
        setLoading(false);
      } catch (err) {
        if (err instanceof DOMException && err.name === "AbortError") return;
        setError("해석이 중단되었습니다.");
        setLoading(false);
      }
    }

    fetchReading();

    return () => controller.abort();
  }, [spread, drawnCards, question]);

  return (
    <div className="mt-6 space-y-3">
      <h3 className="text-lg font-bold">AI 해석</h3>
      <div className="prose prose-sm max-w-none text-gray-700 whitespace-pre-wrap">
        {text}
        {loading && <span className="animate-pulse">▌</span>}
      </div>
      {error && (
        <div className="text-sm text-red-600 bg-red-50 rounded-lg p-3">
          {error}
          <button
            onClick={() => {
              setError(null);
              setText("");
              setLoading(true);
              // Re-trigger by remounting — parent handles this
            }}
            className="ml-2 underline"
          >
            다시 시도
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 커밋**

```bash
git add components/ReadingStream.tsx
git commit -m "feat: add streaming AI reading display component"
```

---

### Task 12: 리딩 페이지 통합 (setup → picking → result)

**Files:**
- Create: `app/spread/[type]/page.tsx`

- [ ] **Step 1: 리딩 페이지 구현**

`app/spread/[type]/page.tsx`:
```tsx
"use client";

import { useState } from "react";
import { useParams, useRouter, notFound } from "next/navigation";
import { spreads } from "@/data/spreads";
import { DrawnCard } from "@/lib/types";
import { autoShuffle, manualPick } from "@/lib/shuffle";
import SetupForm from "@/components/SetupForm";
import ManualPicker from "@/components/ManualPicker";
import SpreadLayout from "@/components/SpreadLayout";
import ReadingStream from "@/components/ReadingStream";

type Phase = "setup" | "picking" | "result";

export default function SpreadPage() {
  const params = useParams();
  const router = useRouter();
  const spread = spreads.find((s) => s.id === params.type);

  const [phase, setPhase] = useState<Phase>("setup");
  const [question, setQuestion] = useState("");
  const [allowReversed, setAllowReversed] = useState(false);
  const [mode, setMode] = useState<"auto" | "manual">("auto");
  const [drawnCards, setDrawnCards] = useState<DrawnCard[]>([]);
  const [readingKey, setReadingKey] = useState(0);

  if (!spread) {
    router.replace("/");
    return null;
  }

  function handleStart(config: {
    question: string;
    allowReversed: boolean;
    mode: "auto" | "manual";
  }) {
    setQuestion(config.question);
    setAllowReversed(config.allowReversed);
    setMode(config.mode);

    if (config.mode === "auto") {
      const cards = autoShuffle(spread.cardCount, config.allowReversed);
      setDrawnCards(cards);
      setReadingKey((k) => k + 1);
      setPhase("result");
    } else {
      setPhase("picking");
    }
  }

  function handleManualComplete(selectedIds: number[]) {
    const cards = manualPick(selectedIds, allowReversed);
    setDrawnCards(cards);
    setReadingKey((k) => k + 1);
    setPhase("result");
  }

  function handleReset() {
    setPhase("setup");
    setDrawnCards([]);
    setQuestion("");
  }

  return (
    <div>
      <button
        onClick={() => router.push("/")}
        className="text-sm text-gray-500 hover:text-gray-700 mb-4"
      >
        ← 스프레드 선택
      </button>

      {phase === "setup" && (
        <SetupForm spreadName={spread.name} onStart={handleStart} />
      )}

      {phase === "picking" && (
        <ManualPicker
          cardCount={spread.cardCount}
          onComplete={handleManualComplete}
        />
      )}

      {phase === "result" && (
        <div>
          <SpreadLayout spread={spread} drawnCards={drawnCards} />
          <ReadingStream
            key={readingKey}
            spread={spread}
            drawnCards={drawnCards}
            question={question}
          />
          <button
            onClick={handleReset}
            className="mt-6 w-full py-3 rounded-lg text-sm font-medium border border-gray-300 hover:border-gray-400 transition-colors"
          >
            다시 뽑기
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 개발 서버에서 전체 흐름 확인**

```bash
npm run dev
```
Expected:
1. 홈에서 스프레드 클릭 → 리딩 페이지
2. 설정 후 "셔플하기" → 결과 + AI 해석 스트리밍
3. 수동 선택 → 78장 그리드 → 선택 후 결과
4. "다시 뽑기" → setup으로 복귀
5. 잘못된 스프레드 URL → 홈으로 리다이렉트

- [ ] **Step 3: 커밋**

```bash
git add app/spread/[type]/page.tsx
git commit -m "feat: add reading page with setup/picking/result phases"
```

---

### Task 13: 카드 이미지 플레이스홀더

카드 이미지(`public/cards/0.webp ~ 77.webp`)가 아직 없으므로, 이미지 로드 실패 시에도 앱이 동작하도록 CardFace에 fallback을 추가한다.

**Files:**
- Modify: `components/CardFace.tsx`

- [ ] **Step 1: CardFace에 fallback 추가**

`components/CardFace.tsx` — Image 태그를 수정:
```tsx
"use client";

import { useState } from "react";
import Image from "next/image";
import { Card } from "@/lib/types";

interface CardFaceProps {
  card: Card;
  reversed: boolean;
  label: string;
}

export default function CardFace({ card, reversed, label }: CardFaceProps) {
  const [imgError, setImgError] = useState(false);

  return (
    <div className="flex flex-col items-center gap-1">
      <div
        className={`w-20 h-36 sm:w-24 sm:h-40 relative rounded-lg overflow-hidden border border-gray-200 shadow-sm ${
          reversed ? "rotate-180" : ""
        }`}
      >
        {imgError ? (
          <div className="absolute inset-0 bg-gray-100 flex items-center justify-center text-xs text-gray-400 p-1 text-center">
            {card.name}
          </div>
        ) : (
          <Image
            src={card.image}
            alt={card.name}
            fill
            className="object-cover"
            sizes="96px"
            onError={() => setImgError(true)}
          />
        )}
      </div>
      <span className="text-xs text-gray-500">{label}</span>
      <span className="text-xs font-medium">
        {card.name}
        {reversed ? " (역)" : ""}
      </span>
    </div>
  );
}
```

- [ ] **Step 2: public/cards 디렉토리 생성**

```bash
mkdir -p public/cards
```

- [ ] **Step 3: 커밋**

```bash
git add components/CardFace.tsx
git commit -m "feat: add image fallback for missing card images"
```

---

### Task 14: 최종 확인 및 빌드 테스트

- [ ] **Step 1: 전체 테스트 실행**

```bash
npm test
```
Expected: 모든 테스트 PASS

- [ ] **Step 2: 프로덕션 빌드 확인**

```bash
npm run build
```
Expected: 빌드 성공, 에러 없음

- [ ] **Step 3: 빌드 실행으로 최종 확인**

```bash
npm run start
```
Expected: localhost:3000에서 전체 흐름 정상 동작

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "chore: verify build and finalize project setup"
```
